<div class="burger">
    <input type="checkbox" id="check" />
    <label for="check" class="checkbtn">
        <i class="fas fa-bars"></i>
    </label>

    <nav class="nav-links-container">
        <div class="nav-mobile">
            <ul class="nav-links">
                <li><a href="#portfolio">Portfolio</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </div>
    </nav>
</div>
<style>
    .nav-links-container {
        display: flex;
        align-items: center;
        gap: 2rem;
    }

    /* nav links horizontal list */
    .nav-links {
        display: flex;
        gap: 2rem;
        list-style: none;
        margin: 0;
        padding: 0;
    }

    /* individual link style + hover underline */
    .nav-links a {
        position: relative;
        text-decoration: none;
        font-weight: bold;
        color: #f36282;
        transition: color 0.3s ease;
        font-family: "Jost", sans-serif;
        font-size: 14px;
    }

    .nav-links a::after {
        content: "";
        position: absolute;
        left: 0;
        bottom: -4px;
        width: 0%;
        height: 2px;
        background: rgb(255, 120, 235);
        transition: width 0.3s ease;
    }

    .nav-links a:hover {
        color: #e3eb00;
    }

    .nav-links a:hover::after {
        width: 100%;
    }
    #check {
        display: none;
    }
    .checkbtn {
        font-size: 1.5rem;
        color: white;
        cursor: pointer;
        display: none;
    }

    .nav-mobile {
        display: none;
        position: absolute;
        top: 4.5rem;
        left: 0;
        background: #46124b;
        z-index: 1;
        width: 200px;
    }

    #check:checked ~ .nav-mobile {
        display: block;
    }

    #check:checked ~ .nav-mobile {
        display: block;
    }

    .nav-mobile a {
        color: #fff;
        padding: 1rem 1.5rem;
        display: block;
    }

    /* hidden by default */

    @media only screen and (max-width: 625px) {
        .checkbtn {
            display: block;
            font-size: 2rem;
            position: absolute;
            top: 30px;
            left: 40px;
        }
        .nav-mobile {
            top: 160px;
            width: 100%;
        }
        .nav-mobile a {
            font-size: 2rem;
            padding: 2rem 3rem;
        }
        .nav-links {
            flex-direction: column;
            gap: 1rem;
        }
    }
</style>
