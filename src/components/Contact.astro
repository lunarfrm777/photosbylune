<div id="contact" class="contact-container">
  <section id="contact" class="contact" tabindex="-3">
    <h2>Contact Me</h2>
    <form>
      <label for="text"></label>
      <input type="text" placeholder="Name" id="text" />

      <label for="email"></label>
      <input type="email" placeholder="Email" id="email" />

      <textarea placeholder="Message"></textarea>

      <button aria-label="Send button">Send</button>
    </form>
  </section>
</div>

<style>
  .contact-container {
    display: flex;
    min-height: 40dvh; /* dvh = dynamic viewport height (modern fix for mobile browsers) */
    padding: 4rem 2rem; /* ⬅️ increase vertical space */
    /* margin-bottom: 1rem; */
    gap: 2rem;
    background-color: #f6ff00;
    padding: 2rem;
    justify-content: center;
  }
  .contact {
    border: 0;
    outline: none;
    background: linear-gradient(
      to right,
      rgba(236, 83, 239, 0.8) 0%,
      rgba(167, 81, 204, 0.8) 96%
    );
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.15);
    color: #fff;
    padding: 12px 16px;
    width: 50%;
    border-radius: 5px;
    margin-top: 25px;
    cursor: pointer;
    font-weight: 500;
    font-family: "Jost", sans-serif;
    transition: 0.3s;
  }

  /* Prevents zoom and stops weird resize */
  input,
  textarea {
    font-size: 16px;
  }
  /* Responsive: stack on smaller screens */

  @media (max-width: 400px) {
    .contact {
      width: 100%; /* ⬅️ key fix */
      padding: 1rem; /* reduce padding */
    }
  }
  @media (max-width: 768px) {
    .contact-container {
      flex-direction: column;
      align-items: center;
    }
  }
  form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  button {
    background: #f97316;
    color: white;
    padding: 0.75rem 1.5rem;
    cursor: pointer;
    transition: background 0.3s;
  }
  button:hover {
    background: #fb923c;
  }
  h2 {
    text-align: center;
  }
</style>
